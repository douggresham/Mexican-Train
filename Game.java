//IMPORTS
public class Game {
	Bufferedreader bufferedreader = new BufferedReader(
	New InputStreamReader(System.in));

	System.out.println('How many players do you have?');
	int numPlayers = bufferedreader.readline();

	System.out.println('Chose color');
	printColors()

	enum Color {
	BLUE,
	GREEN,
	RED,
	PINK,
	PURPLE,
	BLUE,
	YELLOW,
	GREEN,
	RED,
	PINK,
	PURPLE,
	ORANGE,
	SPARKLEY”);

	[]Enum colors = {
	BLUE,
	GREEN,
	RED,
	PINK,
	PURPLE,
	BLUE ,
	YELLOW,
	GREEN,
	RED,
	PINK,
	PURPLe,
	ORANGE,
	SPARKLEY”);
	}

	public static boolean checkColors {
		for color c in Colors
		if c != color {
			return false
		}
		return true

	public static int printAndChooseColorss {
		System.out.println("These are the colors available:")
		for int c = 0; c < colors.length(); c++ {
			System.out.println("" + c +":" " + "colors[c])
		}
		
		for (int i = 1; i <= numPlayers; i++) {
			System.out.println("" + PLAYER + i +" what color do yu prefer?")
			for (int j = 1; j <= numPlayers; j++) { {
				if (j == i0) {
					System.out.pritnln('THE SAME COLOR HHAS BEEN CHOSEN')
					j--)
				elif !checkColors() {
					Sysyetm.out.println("That is not a valid color. They are: ")
				}
				else {
					Sysyetm.out.println("Player " + i + "has been created")
					player0 =  newPlayer()
				}
			}
		int color = Integer.parseInt(bufferedreader.readline());
		System.out.println("Enter the number corresponding to te desired color")
		
		for (int i = 1; j <= 13; i++) { {
			if (j == i0{
			sYSTEM.OUT.PRINTN'THE SAME COLOR HHAS BEEN CHOSEN'
			I--)
			}	
		}
}

